# =====================================================================
# Entorno local para algebra-lineal-sheets
# Uso (con el entorno virtual activado):
#   pip install -r requirements.txt
# =====================================================================

# La libreria (desde PyPI), con soporte de Google Sheets para la clase
# (numpy y openpyxl llegan solos como dependencias del nucleo)
algebra-lineal-sheets[google]>=1.4.0

# Herramientas para ejecutar los notebooks (VS Code / Jupyter)
ipykernel>=6.0
nbconvert>=7.0

# Para los graficos de vectores del notebook de la clase
matplotlib>=3.5

# Para datos con columnas nombradas y regresiones
# (cargar_datos, guardar_datos, matriz_diseno)
pandas>=1.5

# Para la salida profesional de la regresion en el notebook de consumo
# (errores estandar, estadisticos t, p-valores)
statsmodels>=0.14

# NOTA para desarrollo: si vas a MODIFICAR el codigo de la libreria,
# instala tambien la version local en modo editable con:
#   pip install -e .
# (asi tus cambios en algebra_lineal/ se reflejan sin reinstalar)
